.main__messages__box{data: {message: {id: message.id}}}
  .main__messages__box__info
    .main__messages__box__info__name
      = message.user.name
    .main__messages__box__info__date-time
      = message.created_at.strftime("%Y年%m月%d日 %H時%M分")
  .main__messages__box__message
    - if message.content.present?
      %p.main__messages__box__message__text
        = message.content
    = image_tag message.image.url, class: '.main__messages__box__message__image' if message.image.present?